
### Тема 1: Глубокое и поверхностное копирование

**Задание:** Изучить и реализовать концепции глубокого и поверхностного копирования на примере класса `Car`.

```c++
#include <iostream>
#include <cstring>

class Car {
private:
    char *make;
    char *model;
    int year;
    double mileage;

public:
    // Конструктор
    Car(const char *make, const char *model, int year, double mileage)
        : year(year), mileage(mileage) {
        this->make = new char[strlen(make) + 1];
        strcpy(this->make, make);
        this->model = new char[strlen(model) + 1];
        strcpy(this->model, model);
    }

    // Конструктор копирования (глубокое копирование)
    Car(const Car &other)
        : year(other.year), mileage(other.mileage) {
        make = new char[strlen(other.make) + 1];
        strcpy(make, other.make);
        model = new char[strlen(other.model) + 1];
        strcpy(model, other.model);
    }

    // Деструктор
    ~Car() {
        delete[] make;
        delete[] model;
        std::cout << "Car object has been destroyed." << std::endl;
    }

    // Метод вывода информации
    void displayInfo() const {
        std::cout << "Car: " << make << " " << model << " (" << year << "), Mileage: " << mileage << std::endl;
    }
};

// Основная функция
int main() {
    Car car1("Toyota", "Camry", 2020, 15000);
    Car car2 = car1;  // Вызов конструктора копирования
    car2.displayInfo();
    return 0;
}

```
### Тема 2: Наследование, композиция, агрегация, ассоциация, зависимость

**Задание:** Изучить и реализовать концепции наследования, композиции, агрегации, ассоциации и зависимости на примере классов: `Student`, `Book`, `Computer`, `BankAccount`, `Phone`, `Player`, `Movie`, `Worker`, `Product`.

#### План работ для каждого задания:

1. **Класс `Student` и `InternationalStudent`:**
    
    - Создать базовый класс `Student` с полями `name`, `age`, `major`.
    - Создать класс `InternationalStudent`, который наследует `Student` и добавляет поля `country` и `language`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Понять, как работает наследование.
2. **Класс `Book` и `Textbook`:**
        - Создать базовый класс `Book` с полями `title`, `author`.
    - Создать класс `Textbook`, который наследует `Book` и добавляет поля `subject` и `edition`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Понять, как использовать наследование для расширения функциональности.
3. **Класс `Computer` и `DesktopComputer`:**
    - Создать базовый класс `Computer` с полями `brand`, `processor`.
    - Создать класс `DesktopComputer`, который наследует `Computer` и добавляет поля `monitorSize` и `isGamingPC`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Освоить наследование и его применение в производных классах.
4. **Класс `BankAccount` и `CheckingAccount`:**
    - Создать базовый класс `BankAccount` с полями `accountNumber`, `balance`.
    - Создать класс `CheckingAccount`, который наследует `BankAccount` и добавляет поле `overdraftLimit`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Изучить наследование с добавлением специфичных полей.
5. **Класс `Phone` и `FeaturePhone`:**
    - Создать базовый класс `Phone` с полями `brand`, `model`.
    - Создать класс `FeaturePhone`, который наследует `Phone` и добавляет поля `hasCamera` и `hasBluetooth`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Понять, как расширять функциональность базового класса.
6. **Класс `Player` и `ProfessionalPlayer`:**
    - Создать базовый класс `Player` с полями `name`, `score`.
    - Создать класс `ProfessionalPlayer`, который наследует `Player` и добавляет поля `team` и `salary`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Изучить применение наследования для создания специализированных классов.
7. **Класс `Movie` и `Documentary`:**
    - Создать базовый класс `Movie` с полями `title`, `director`.
    - Создать класс `Documentary`, который наследует `Movie` и добавляет поля `subject` и `duration`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Изучить расширение базового класса для специализированных категорий.
8. **Класс `Worker` и `Manager`:**
    - Создать базовый класс `Worker` с полями `name`, `salary`.
    - Создать класс `Manager`, который наследует `Worker` и добавляет поля `department` и `bonus`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Понять, как создавать иерархию классов.
9. **Класс `Product` и `PerishableProduct`:**
    - Создать базовый класс `Product` с полями `name`, `price`.
    - Создать класс `PerishableProduct`, который наследует `Product` и добавляет поля `expirationDate` и `storageConditions`.
    - Реализовать методы вывода информации для обоих классов.
    - **Цель:** Понять, как использовать наследование для спецификации продуктов.
10. **Композиция: Классы `Library` и `Book`:**
    - Создать класс `Library`, который содержит массив объектов `Book`.
    - Реализовать методы добавления и вывода информации о книгах в библиотеке.
    - **Цель:** Понять, как работает композиция.
11. **Агрегация: Классы `Course` и `Student`:**
    - Создать класс `Course`, который содержит список студентов (использовать указатели).
    - Реализовать методы для добавления и вывода студентов в курсе.
    - **Цель:** Понять разницу между агрегацией и композицией.
12. **Ассоциация: Классы `Teacher` и `Student`:**
    - Создать класс `Teacher`, который имеет ссылку на объект `Student`.
    - Реализовать метод, который позволяет учителю оценивать студента.
    - **Цель:** Понять ассоциацию и ее реализацию.
13. **Зависимость: Класс `EmailSender`:**
    - Создать класс `EmailSender`, который зависит от класса `User` для отправки писем.
    - Реализовать метод отправки письма, принимая объект `User` как параметр.
    - **Цель:** Понять, как класс может зависеть от другого класса.

### Тема 3: Полиморфизм

1. Создать базовый класс `Animal` с методом `sound()`.
2. Создать производные классы `Dog` и `Cat`, переопределяющие метод `sound()`.
3. Создать массив указателей на `Animal` и заполнить его объектами `Dog` и `Cat`.
4. Вывести звуки, используя полиморфизм.